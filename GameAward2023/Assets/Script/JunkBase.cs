using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class JunkBase : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
		//Rigidbody rigidbody = this.transform.GetComponent<Rigidbody>();
		//float currentSpeed = rigidbody.velocity.magnitude;
		//
		//if (currentSpeed >= 5.0f)
		//{
		//	//Vector3 currentVelocity = rigidbody.velocity.normalized;
		//	//Vector3 limitedVelocity = currentVelocity * 5.0f;
		//	//rigidbody.velocity = limitedVelocity;
		//}
		//else
		//{
		//	rigidbody.AddForce(this.transform.forward.normalized * 50.0f);
		//}
	}
}
